<!--
Copyright (C) 2026 SharpEmu Emulator Project
SPDX-License-Identifier: GPL-2.0-or-later
-->

# ASTRO BOT Title/Menu Progress

This journal is the durable source of truth for the ASTRO BOT title/menu investigation. Update it before starting a new experiment and after recording the result so failed or superseded approaches are not repeated.

## Objective and success criteria

- Boot ASTRO BOT PPSA21564 v01.007.000 to a correctly rendered title/menu.
- Use the original guest vertex and pixel shaders; no solid-color, early-return, shader-override, or title-only depth bypasses.
- Keep the menu visible for at least 60 seconds and verify that it accepts input.
- Finish with a Release build, `git diff --check`, reproducible evidence, and clean commits.

## Working state

- Checkout: `/Users/mcruz/Developer/sharpemu-astro-playable-next`
- Active branch: `codex/astro-playable-next`; fork `main` was at `670a0ff` before the 2026-07-16 upstream sync.
- Current upstream sync target: `par274/sharpemu` `origin/main` at `2db1fae`.
- Previous clean ASTRO checkpoint: `670a0ff [AGC/Vulkan] Restore ASTRO title scene production`.
- Validated 2026-07-16 sync checkpoint: `7bcbd4e [Sync] Merge par274/main into ASTRO progress`.
- Historical depth checkpoint: `ed283b4 [astro] Checkpoint title depth diagnosis`
- Generic depth fix: `e98f298 [astro] Neutralize uninitialized read-only depth`
- Base work: `xnetcat/superliminal-launch` plus PR #149 multi-render-target support.
- Game:
  `/Volumes/Untitled/games/sharpemu/Games/Astro Bot/PPSA21564 [ 01 007 ]/PPSA21564-app-1/eboot.bin`
- Required runtime compatibility flags:
  `SHARPEMU_IGNORE_STACK_CHK=1 SHARPEMU_IGNORE_INT41=1 SHARPEMU_GPU_WAIT_MODE=force`
- Title targets: MRT0 `0x514080000`, MRT1 `0x512570000`, both 1920x1080 R16G16B16A16Sfloat.
- Title depth: `0x513560000`; decoded extent is stale 1x1 and repaired to 1920x1080.
- Title shaders: ES `0x500780000`, PS `0x500781200`; 24,192 indexed vertices.

## Confirmed milestones

- Boot proceeds through initialization and AVPlayer intro decode.
- The guest loads `title_controller_ship [title]` and reports `LoadLevelResources` completion.
- The Vulkan path executes more than 1,300 translated draws using real render targets, textures, vertex/index buffers, and depth attachments.
- PR #149 typed MRT support is integrated and produces true two-attachment draws.
- Missing imports implemented from `Firmware/nids.csv`:
  `sceAmprCommandBufferGetNumCommands`, `sceSystemServiceGetHdrToneMapLuminance`, and `sceVideoOutGetVblankStatus`.
- The title draw and its immediate MRT readbacks are reproducibly identifiable.
- Original guest shaders now render recognizable boot/title content: first 3D scene geometry and then the centered PlayStation Studios logo.
- `sce::Json` room metadata now parses far enough for the game to report `GAME: Level has started: title_controller_ship`.

## Current diagnosis

The first proven blocker was depth initialization, not an inability to rasterize the title geometry. The unmodified title draw enables depth testing, disables depth writes, and uses LEqual. Its repaired 1920x1080 host depth image is created for the first time at the title draw and inherits guest `DB_DEPTH_CLEAR=0.0`, so every fragment was rejected. Generic first-use depth handling now selects the neutral value 1.0 for an uninitialized read-only LEqual test while retaining the guest clear value for later real initialization.

The generic first-use depth blocker is resolved. The title shader investigation also proved that translated SPIR-V, MRT routing, packed-half exports, stage linkage, and sampled color can all produce nonzero data. Controlled color injection isolated the remaining black-frame boundary to final pixel shader `0x500640200`. Forced color at composite PS `0x5006CD700` survives composite rasterization and upscaler CS `0x50068EF00`, producing a fully nonblack 2432x1368 input. Capturing the final PS's first image sample at PC `0x2C` also produces a fully nonblack flip buffer, proving its image ownership, descriptor binding, and coordinates are correct.

The newest generic fix corrects GFX10 `VCMPX` lowering: `VCMPX` writes EXEC only and must not overwrite the decoded SDWA scalar register pair. The shader saves EXEC in `s38:s39`, copies it to `s106:s107`, performs three conditional `VCMPX` regions, and restores EXEC from `s106:s107`; SharpEmu had destroyed that saved mask with each compare. With the correction, original shaders are no longer uniformly black, but only 4,823 dim pixels survive in a stable two-scanline strip at `y=0..1`. This is not the menu. Post-fix markers prove EXEC is active after the first sample and at pack convergence. The live scalar branch follows the long path through PC `0x63C`, not the previously suspected short path through PC `0x170`. Multi-point capture isolates the first magnitude collapse to `VMadF32 v4 <- v9,v4,v11` at PC `0x40C`: its input `v4` is one, its small addend `v11` is about 0.004, and multiplier `v9` from `ImageLoad` PC `0x31C` is zero. The bound 1x1 texture's first component is demonstrably float one. The descriptor is type 8 (1D), but SharpEmu accepted only type 9 (2D), so it replaced this real image with a zero fallback before upload. Allowing 1D images through the common height-1 Vulkan path now preserves the address and seeds the correct nonzero 16-byte texel. This produced the first visibly rendered 3D title geometry and a recognizable PlayStation Studios boot logo from original shaders. Both are dark blue with strong vertical striping, so the remaining rendering defect is color/texture/composite interpretation rather than missing geometry or rasterization. A separate generic `sce::Json` HLE now carries the room loader through its animation/material documents to `GAME: Level has started: title_controller_ship`; a clean sustained run must determine whether the intro continues to the ASTRO BOT title/menu before another shader probe is introduced.

A later title-loop draw was definitely lost: ES `0x5003EDF00` plus PS `0x5003EE900`, six vertices, failed translation every frame on VOP2 opcode `0x2D`. LLVM's GFX10 instruction definitions identify `0x2B`, `0x2C`, and `0x2D` as `V_FMAC_F32`, `V_FMAMK_F32`, and `V_FMAAK_F32`. SharpEmu was using the older `V_LDEXP_F32` meaning for `0x2B` and did not decode the two literal FMA forms. The decode fix now translates and executes this draw with no shader translation misses. It exposed a second generic bug: the resulting `R8G8B8A8Unorm` target was discarded when sampled through a legal `R8G8B8A8Srgb` mutable view. The compatibility fix now produces a cache hit, and address-filtered readback proves a forced color survives both writes to the 4K target. That auxiliary 4K path still does not feed the observed title presentation, so it is no longer the primary title/menu lead; return to the 1920x1080 title graph with both generic fixes retained.

## Experiment log

| ID | Hypothesis | Exact change or flags | Observed result | Conclusion | Evidence |
| --- | --- | --- | --- | --- | --- |
| E01 | Full Gen5 register defaults are required for real targets. | Added primary-register defaults and invalid-register tracing. | Invalid indirect registers fell from 7,839 to 36; targets became 3840x2160/1920x1080 instead of placeholders. | Register defaults are necessary and retained. | Commit `1caa668` and earlier logs. |
| E02 | PR #149 is required for the title's two color outputs. | Integrated commits `a45a20e` and `8dd64ed`. | Vulkan executes `mrt=2` with two R16G16B16A16Sfloat targets. | Typed MRT support is necessary and retained. | Commits `a45a20e`, `8dd64ed`. |
| E03 | Missing title imports block progress. | Implemented three NIDs using `Firmware/nids.csv`. | Game reaches and loads `title_controller_ship`. | Imports were real boot blockers; resolved. | Checkpoint `ed283b4`. |
| E04 | Packed-half pixel exports are writing zero. | Added packed-half shadow registers and export tracing; forced constants at selected exports. | Normal title targets stayed black. | Inconclusive because depth rejected all fragments. Do not repeat until depth passes. | `/tmp/astro-pr149-title-single-dump3.log`. |
| E05 | Large/dead SPIR-V or MRT1 declaration prevents output. | Reduced the title PS to a validated 352-byte single-MRT magenta module and injected it. | Both targets stayed black. | Shader size and MRT1 were not the immediate cause; depth still rejected fragments. | `/tmp/astro-pr149-title-single-inject.log`. |
| E06 | The generated SPIR-V differs from the known-working fixed shader. | Dumped the 316-byte fixed solid fragment and injected those exact bytes through the normal pixel field. | Title targets stayed black. | Fragment module bytes were not the immediate cause. | `/tmp/astro-pr149-title-fixed-inject.log`. |
| E07 | Address-targeted fixed fragments prove the title draw itself works. | `SHARPEMU_FORCE_SOLID_FRAGMENT_TARGETS=0x514080000,0x512570000`. | Targets became magenta. | Superseded conclusion: the flag also changed earlier passes and depth history, so this did not isolate the title draw. | `/tmp/astro-pr149-title-force-solid-recheck.log`. |
| E08 | Replacing only the title fragment shader is sufficient. | `SHARPEMU_FORCE_TITLE_SOLID_FRAGMENT=1`. | Immediate title MRT readbacks stayed zero. | The title draw produced no surviving fragments with original depth state. | `/tmp/astro-pr149-title-only-solid.log`. |
| E09 | Original non-depth render state suppresses the title. | Title-only solid fragment plus default blend, viewport/scissor, raster, and depth. | MRT0 filled 1920x1080 magenta. | Vertex geometry and raster path are valid; one decoded state is suppressing output. | `/tmp/astro-pr149-title-solid-default-state.log`. |
| E10 | Depth alone suppresses the title draw. | Title-only solid fragment plus `SHARPEMU_FORCE_TITLE_DISABLE_DEPTH=1`; all other state original. | MRT0 filled 1920x1080 magenta. | Confirmed: depth is the first blocker. | `/tmp/astro-pr149-title-solid-no-depth.log`. |
| E11 | Earlier depth-only rendering initializes `0x513560000`. | Added stale-extent inference for depth-only passes and enabled AGC/depth lifecycle trace. | The 1920x1080 depth image was first created at the title draw, clear=0; no earlier translated depth-only writer populated it. | Extent repair alone cannot resolve first-use depth contents. | `/tmp/astro-pr149-depth-extent-fix-solid.log`. |
| E12 | A clean four-case matrix will separate depth and pixel failures. | M1 original/original; M2 solid/original; M3 solid/depth-disabled; M4 original/depth-disabled. All used title-only controls and immediate MRT readback. | M1 black, M2 black, M3 MRT0 full magenta, M4 black. | Depth and translated pixel exports are independent blockers. Do not repeat the module-size, single-MRT, or fixed-SPIR-V probes until first-use depth is fixed. | `/tmp/astro-matrix-m1-original-depth.log` through `/tmp/astro-matrix-m4-original-no-depth.log`. |
| E13 | Neutral first-use depth initialization will make the original LEqual state pass without a bypass. | Added generic depth initialization source tracking and neutral clear selection; ran solid and original shaders with normal depth plus `SHARPEMU_TRACE_DEPTH_INIT=1`. | Trace reports guest_clear=0, effective_clear=1, compare=3. Solid MRT0 becomes full magenta; original shader remains uniform black. | Depth blocker resolved generically. Pixel export translation is now the active blocker. | `/tmp/astro-depth-neutral-solid.log`, `/tmp/astro-depth-neutral-original.log`. |
| E14 | A translator-generated early color proves the translated pixel pipeline and output store independently of guest execution. | Original title draw and normal depth with `SHARPEMU_FORCE_TITLE_EARLY_COLOR=1`; immediate MRT readback enabled. | Depth trace selects 1.0 for LEqual; MRT0 fills 1920x1080 magenta (`6220800` nonzero bytes, `2073600/2073600` nonblack pixels, hash `E101B0AC71122325`); MRT1 remains zero. | Translator-generated SPIR-V, pipeline creation, depth, rasterization, and the basic output store work. Continue at guest execution and packed-half export data. | `/tmp/astro-pixel-early-color.log`. |
| E15 | If the original exports execute but their computed packed-half values are wrong, forcing only those values to one will produce nonzero MRT data. | Original title shader and normal depth with `SHARPEMU_FORCE_PACKED_EXPORT_ONE=1`, `SHARPEMU_TRACE_PACKED_EXPORT=1`, and immediate MRT readback. | MRT0 becomes uniform `(1,1,1,1)` (`8294400` nonzero bytes, hash `17421844C4132325`); MRT1 becomes `(1,1,0,0)` (`4147200` nonzero bytes, hash `E7CEEE6454A36325`). MRT1 components sourced through the later `VMovB32 v3` do not use the packed shadow and remain zero. | Original export instructions execute and EXEC permits their stores. The active fault is real packed-half value computation/propagation, especially shadow provenance across VGPR moves. | `/tmp/astro-pixel-packed-one.log`. |
| E16 | The final pack stores may be suppressed because EXEC is inactive before the guest restores its export mask. | Original shader and normal depth with `SHARPEMU_FORCE_PACKED_STORE_EXEC_VALUES=1`; the diagnostic stores `1.0` when pack-time EXEC is true and `0.5` when false. | MRT0 is uniform `(0.5,0.5,0.5,0.5)` and MRT1 `(0.5,0.5,0,0)`. The later `VMovB32 v3, src[128]` is an intentional inline zero. | All final pack instructions execute with translator `_exec=false`; export-time EXEC is later restored and works. Trace the preceding `VCmpxGtF32` and sampled texture/control inputs instead of changing packed-half propagation. | `/tmp/astro-pixel-packed-store-exec.log`. |
| E17 | The black 1x1 sampled texture may make the comparison chain reject every title fragment. | Original shader and normal depth with `SHARPEMU_FORCE_WHITE_TEXTURE_TARGETS=0x571740F00`; no export or EXEC overrides. | The texture upload is forced to all `0xFF`, but both immediate title MRT readbacks remain uniform zero. | Texture contents alone do not explain the empty EXEC mask. Isolate the two final `VCmpxGtF32` decisions directly. | `/tmp/astro-title-white-texture-rerun.log` (the first attempt exited before the title and is non-evidence). |
| E18 | One of the final `VCmpxGtF32` instructions empties EXEC. | Added temporary title-PS controls for PCs `0x4D4` and `0x540`; tested each independently and together with an explicitly rebuilt `osx-x64` Release binary. | Forcing `0x4D4` alone stays black. Forcing `0x540` alone produces MRT0 `(0,0,0,10000)` (`4147200` nonzero bytes, hash `AB8D9CA9A7FFA325`), identical to forcing both; MRT1 stays zero. | The `0x540` comparison is the immediate EXEC killer and `0x4D4` already passes. Its operands and the output color inputs are zero/missing upstream. | `/tmp/astro-title-force-compare-4d4-x64.log`, `/tmp/astro-title-force-compare-540-x64-rerun.log`, `/tmp/astro-title-force-compare-both-x64.log`. |
| E19 | The known title vertex-color attribute is zero and causes the final comparison/color outputs to collapse. | `SHARPEMU_FORCE_TITLE_VERTEX_COLOR_WHITE=1`, tested with original comparisons and again with `SHARPEMU_FORCE_TITLE_COMPARE_540=1`. | The override triggers for the 67,568-byte loc0 buffer, but the original shader stays black; with `0x540` forced, MRT0 remains exactly `(0,0,0,10000)` and MRT1 zero. | The loc0 color attribute is not the missing input. Inspect vertex export/pixel interpolation interfaces and global buffers. | `/tmp/astro-title-vertex-white.log`, `/tmp/astro-title-vertex-white-force-540.log`. |
| E20 | Vertex parameter exports and pixel interpolation locations do not match. | Added `SHARPEMU_TRACE_TITLE_INTERFACE=1` for the title ES/PS and dumped export/interpolation controls. | ES exports targets 32, 33, 34, 35 with masks `F,F,F,3`; PS reads attributes 0, 1, 2, 3. The vertex compiler is given four required outputs. | Stage locations match exactly. Isolate vertex output computation from downstream pixel computation by forcing only the four parameter exports. | `/tmp/astro-title-interface.log`. |
| E21 | The real vertex parameter values are zero and block pixel color. | Added `SHARPEMU_FORCE_TITLE_VERTEX_OUTPUTS_ONE=1`; tested it alone, with forced `0x540`, with the white texture, and compared against white texture plus forced `0x540` using original vertex outputs. | Vertex constants alone stay black; with `0x540` they still yield only `(0,0,0,10000)`. White texture plus `0x540` yields `(1,1,1,10000)` with or without forced vertex outputs (`10368000` nonzero bytes, hash `8942F634A0860325`). | Original vertex outputs and stage linkage are sufficient. The decisive inputs are the black sampled RGB and the `0x540` comparison operands; continue in the pixel shader. | `/tmp/astro-title-force-vertex-outputs.log`, `/tmp/astro-title-force-vertex-outputs-540-rerun2.log`, `/tmp/astro-title-force-all-inputs.log`, `/tmp/astro-title-white-texture-540.log`. |
| E22 | The final compare operands are nonzero but mishandled by compare translation. | Address-filtered `SHARPEMU_FORCE_PIXEL_EXPORT_VGPR_BASE=11` with only `0x540` forced so MRT0 exposes `v11..v14` and MRT1 exposes `v15..v18`. | MRT0 is uniform `(0,0,1,1)` and MRT1 `(0,1,0,0)`. Thus `v12=0` and `v11=0`, so `VCmpxGtF32 v12,-v11` correctly evaluates `0 > -0` as false. | Compare semantics are correct; trace the buffer/resource calculations that collapse both operands to zero. | `/tmp/astro-title-export-vgpr11.log`. |
| E23 | Missing or misbound scalar buffers collapse the compare operands. | Extended the title interface trace to map every `SBufferLoad` to binding, guest base, offset, and bytes. | All 19 unique PS loads resolve to valid in-range guest data. PC `0x504` reads `0000803F 0000803F 0000803F 00000000`, so `s3=0`; the following guest instructions mathematically produce `v12=0`. | The zero/discard is driven by guest material data, not a missing binding. Treat this specific draw as likely intentionally invisible and return to the full presentation graph. | `/tmp/astro-title-scalar-bindings.log`. |
| E24 | Presentation or swapchain conversion turns a valid scene black. | Original shaders with `SHARPEMU_TRACE_GUEST_IMAGES=present`, `SHARPEMU_SWAPCHAIN_DUMP_EVERY=120`, and `SHARPEMU_LOG_AGC_SHADER=1`; then a narrow `SHARPEMU_TRACE_COMPUTE_SHADER_ADDRESS=0x50068EF00` rerun. | Direct guest display buffers and swapchain samples are uniformly zero. At title time CS `0x50068EF00` repeatedly reads `0x537060000` and writes `0x53AA00000`; the input is already black. `0x537060000` is written by composite PS `0x5006CD700` from the black 1920x1080 title graph. | Presentation is not the black-frame source. Follow the actual title producers before touching swapchain code. | `/tmp/astro-presentation-chain.log`, `/tmp/astro-compute-68ef-title-long.log`. |
| E25 | The title material alpha is a fade value that should become nonzero after loading. | Added `SHARPEMU_TRACE_TITLE_GLOBALS_LIVE=1` and sampled binding 1 offset `0x4340` on every evaluation of PS `0x500781200`. | All 198 probes across 99 title frames are exactly `0000803F 0000803F 0000803F 00000000`, across both guest buffer bases used by the paired draws. | The zero alpha is real and stable, not a stale one-time scalar evaluation or an unfinished fade. Keep it as a secondary question while checking dropped title draws. | `/tmp/astro-title-globals-live.log`. |
| E26 | A missing GFX10 literal-FMA opcode drops the menu/UI draw. | Corrected VOP2 `0x2B` to `VFmacF32` and added `0x2C`/`0x2D` as `VFmaMkF32`/`VFmaAkF32`, including mandatory literal decoding and fused multiply-add SPIR-V lowering; ran original shaders with normal depth, `SHARPEMU_LOG_AGC_SHADER=1`, presentation tracing, and 120-frame dumps. | Release `osx-x64` build succeeds. PS `0x5003EE900` translates with a 21,944-byte module and executes for at least 17 consecutive frames; `unknown-vop2 op=0x2D` and all `agc.shader_translate_miss` records are gone. It writes target `0x5794C0000`, but the subsequent texture-source probe reports `nonzero64=0`. The process later ends in a guest access violation before a post-draw swapchain checkpoint. | The opcode bug and dropped draw are fixed, but this draw still produces black. Probe its export/raster path directly; do not infer presentation success from successful translation. | `/tmp/astro-build-gfx10-fma.log`, `/tmp/astro-gfx10-fma-runtime.log`; LLVM `VOP2Instructions.td` GFX10 definitions. |
| E27 | The newly translated UI draw renders, but its UNORM target is discarded when the next pass samples it as SRGB. | Forced magenta only at PS `0x5003EE900` exports using `SHARPEMU_FORCE_PIXEL_MAGENTA=1 SHARPEMU_FORCE_PIXEL_EXPORT_ADDRESS=0x5003EE900`; dumped its IR/SPIR-V; then added `R8G8B8A8Srgb` to the mutable-image compatibility class and reran original shaders with resource tracing. | Before the fix, target `0x5794C0000` is rendered as `R8G8B8A8Unorm` but the consumer requests `R8G8B8A8Srgb`; no cache hit occurs and a separate texture is uploaded from black guest RAM. After the fix, the consumer logs `vk.texture_alias_view` and `vk.texture_cache_hit` for the same host image. The original presented frame remains black. | Confirmed and fixed a generic render-to-texture alias blocker. It is necessary but not sufficient for the title/menu. | `/tmp/astro-ui-magenta.log`, `/tmp/astro-srgb-alias.log`; `shader-dumps/00000005003EE900-8547012B00BB5556.ps.{ir.txt,spv}`. |
| E28 | Address-filtered magenta does not appear because the six-vertex draw fails to rasterize or its following pass clears the target. | Alias fix active; `SHARPEMU_FORCE_PIXEL_MAGENTA=1 SHARPEMU_FORCE_PIXEL_EXPORT_ADDRESS=0x5003EE900 SHARPEMU_TRACE_GUEST_WRITES=0x5794C0000`; dumped paired ES `0x5003EDF00`. | Each of the first three direct target readbacks is full 3840x2160 magenta: `8,294,400/8,294,400` nonblack pixels, center `FF00FFFF`, hash `B002A4707020A325`. The intervening zero-write-mask pass preserves the image. Presentation still stays black in the separate 10-frame run. | ES geometry, rasterization, PS EXEC/export, target preservation, and UNORM/SRGB aliasing all work. This 4K path is auxiliary to the black title graph, so stop using it as the primary menu lead. | `/tmp/astro-srgb-target-readback.log`, `/tmp/astro-srgb-magenta.log`; `shader-dumps/00000005003EDF00-A8AEECCD408C1A2C.vs.{ir.txt,spv}`. |
| E29 | If the first title PS is the sole missing producer, forcing its output should survive the later composite. | `SHARPEMU_FORCE_PIXEL_MAGENTA=1 SHARPEMU_FORCE_PIXEL_EXPORT_ADDRESS=0x500781200`, with direct readback of the first `0x5006CD700` composite outputs. | Both 2432x1368 composite targets retain zero RGB and alpha 1; center `000000000000003C`, hash `F0AF3F03C2E22325`. | That controller-ship draw is not selected into the final composite by itself; continue downstream with controlled injection. | `/tmp/astro-title-magenta-composite.log`. |
| E30 | The composite raster/export path is the first black-frame boundary. | Forced magenta only at PS `0x5006CD700` and traced `0x537060000,0x564000000`. | Composite output is full nonblack at 2432x1368: `3,326,976/3,326,976` pixels, center `003C0000003C003C`, hash `2F10767CC0922325`; the normal swapchain remains black. | Composite rasterization and exports work. Follow the injected color through upscaling and the final pass. | `/tmp/astro-composite-magenta.log`. |
| E31 | Upscaler CS `0x50068EF00` drops the nonblack composite input. | Forced composite PS `0x5006CD700`; traced CS `0x50068EF00`, address `0x53AA00000`, and presentation. | The CS reads `0x537060000` and its 2432x1368 output is fully nonblack: `3,326,976/3,326,976` pixels, center `FE3B0080FE3B003C`, hash `490DC9B46D272325`; the swapchain is still black. | The upscaler works. The remaining boundary is the final 4K raster pass or presentation. | `/tmp/astro-upscaler-output.log`. |
| E32 | The real final PS writes black even when its main upscaled input is nonblack. | Forced composite PS `0x5006CD700`; traced the 42nd 110,496-byte PS write, corresponding to PS `0x500640200` and flip target `0x5093F0000`. | The final draw samples nonblack `0x53AA00000:2432x1368` but writes a uniform-black 3840x2160 A2R10G10B10 target: `0/8,294,400` nonblack pixels, hash `7F2C361DEB0F2325`. | Final PS `0x500640200` or its other bound inputs are the active blocker; presentation is not yet involved. | `/tmp/astro-final-pass-readback.log`. |
| E33 | A final-PS-only export override will prove the flip and swapchain route. | `SHARPEMU_FORCE_PIXEL_MAGENTA=1 SHARPEMU_FORCE_PIXEL_EXPORT_ADDRESS=0x500640200`, address/presentation tracing, and 10-frame dumps. | Final target `0x5093F0000` is full nonblack (`8,294,400/8,294,400`, center `FF03F0FF`, hash `8DDBE441AD2A6325`). Every captured 2560x1440 swapchain frame is full nonblack with stable hash `1A671F045ECC2325`; visual capture is solid magenta. | Final geometry/raster/export, flip capture, scaling, and presentation all work. Inspect the real final shader's texture values, control flow, and translation. | `/tmp/astro-final-pass-magenta.log`, `/tmp/astro-final-pass-magenta.png`. |
| E34 | Kyty's explicit subgroup-size handling may explain the black compute/final passes. | Queried `PhysicalDeviceSubgroupProperties` and `PhysicalDeviceSubgroupSizeControlProperties` on the active Apple M3 Max MoltenVK device; reviewed SharpEmu's wave64 bridge. | Default subgroup size is 32; supported size range is 4-32, but `required_stages=None`, so the host cannot request a different size. The translator already combines two 32-lane subgroups for 64-lane 8x8 compute waves. | Kyty's warning is valid generally, but simply enabling subgroup-size control cannot change this device. Retain wave64 bridge correctness as a possible shader-level issue. | `/tmp/astro-subgroup-probe.log`; `OtherPS5Emulators/KYTY_BLACK_SCREEN_RESEARCH.md`. |
| E35 | The downsample/upsample chain overwrites the nonblack `0x53AA00000` before final presentation. | Forced composite PS `0x5006CD700`; added occurrence-filtered storage-image readback and captured the first title dispatches of CS `0x500655600`, `0x500656600`, and `0x500657200`. | CS `0x500655600` produces black `0x53CC90000`, and every smaller downsample is black. The final 2432x1368 `0x53AA00000` read after CS `0x500657200` is nevertheless fully nonblack with the same hash `490DC9B46D272325` as the injected source. Resource tracing separately confirms the final PS gets an exact initialized 2432x1368 cache hit. | The final pass has a live nonblack host image even though CPU-side guest probes are stale black. Capture the PS's actual sampled value before blaming its later tone-map math. | `/tmp/astro-compute-chain-first-title.log`, `/tmp/astro-srgb-alias.log`. |
| E36 | Final PS `0x500640200` samples black or the wrong image at PC `0x2C`. | Forced composite PS `0x5006CD700` magenta; captured the PC `0x2C` image-sample result into `v248..v251` and routed those VGPRs through the final PS export. | Before the title transition the diagnostic output is correctly RGB black with alpha one. Once the title resources are live, the 3840x2160 final target is fully nonblack (`8294400/8294400` pixels, center `FE03E0FF`, hash `83AE6A3556B5A325`) and the swapchain is stably fully nonblack (hash `1A671F045ECC2325`) across many frames. | Image ownership, cache identity, descriptor binding, sample coordinates, and the PC `0x2C` sample instruction are working. The real final PS turns a valid nonblack sample black later in its math or EXEC/control flow. | `/tmp/astro-final-raw-sample.log`. |
| E37 | The final tone-map values become zero before half-float packing. | Added address/PC-targeted VGPR snapshots, path markers, and two-point EXEC probes around final PS PCs `0x170` and `0x80C`; compared EXEC-guarded and unguarded captures while forcing composite PS `0x5006CD700` magenta. | Unguarded snapshots expose nonzero late RGB values, while guarded pack-time snapshots remain black. IR provenance shows `s38:s39` saves the original EXEC mask, PC `0x3F0` copies it to `s106:s107`, and PCs `0x4DC`, `0x504`, and `0x52C` restore EXEC from that pair. SharpEmu instead overwrote `s106:s107` at every `VCMPX`. | The packed values are present; incorrect GFX10 `VCMPX` scalar-destination lowering suppresses their stores. GFX10 `VCMPX` is EXEC-only, so retain `s106:s107` and update EXEC alone. | `/tmp/astro-final-pack-inputs-unmasked-retry.log`, `/tmp/astro-final-path-markers-unmasked.log`, `/tmp/astro-final-exec-two-point.log`; LLVM `VOPCInstructions.td` and Mesa's GFX10 `vcmpx_exec_only` handling. |
| E38 | Correct GFX10 `VCMPX` EXEC-only semantics are sufficient for the original title frame. | Changed `TryEmitVectorCompare` so `VCmpx*` updates only EXEC; ordinary `VCmp*` still writes VCC or its explicit SDWA scalar destination. Ran both the forced-composite control and an original-shader run with no color/VGPR/export overrides. | Both runs change from uniform black to the same stable swapchain hash `05022A3EB90FE861`, with 4,823 nonblack pixels. Raw BGRA analysis shows every nonblack pixel is dim grayscale and confined to the first two scanlines (`bbox=0,0-2559,1`). | The generic `VCMPX` fix is necessary and retained, but it does not render the menu. Probe post-fix EXEC coverage and packed values on the live control-flow path; the two-row output is the new earliest visible symptom. | `/tmp/astro-final-vcmpx-fixed-composite.log`, `/tmp/astro-vcmpx-original.log`, `/tmp/astro-vcmpx-original-dumps/present-0020-2560x1440-B8G8R8A8Unorm.bgra`. |
| E39 | The short tone-map path through PC `0x170` executes with an empty EXEC mask and clears the sampled RGB. | Forced composite PS `0x5006CD700`; final PS `0x500640200`; captured EXEC after PCs `0x2C`, `0x70`, `0x170`, and `0x80C` into `v248..v251`, then exported those VGPRs. | The A2R10 target is uniform `center=0000F0FF`, hash `19278AFB8578A325`: R and A are one while G and B are zero. Thus PC `0x2C` and PC `0x80C` are active, while the PC `0x70` and PC `0x170` captures were never reached. Re-reading E38's path-marker target as A2R10 shows `center=FF030000` sets B, so its active marker was `v250` = PC `0x63C`, not `v248` = PC `0x170`. | Corrected conclusion: EXEC is healthy at the input and convergence; the live shader takes the long path through PC `0x63C`. The PC `0x70`/`0xB8`/`0xEC` short-path zero probes were dead-path evidence and must not guide the fix. Capture `v10`, `v12`, and `v9` immediately after PC `0x63C`, then search backward only on the live long path. | `/tmp/astro-vcmpx-short-exec-path.log`, `/tmp/astro-vcmpx-postfix-path.log`. |
| E40 | The long-path RGB remains valid until late math near PC `0x63C`. | Added temporary `SHARPEMU_CAPTURE_PIXEL_VGPR_POINTS=pc:source:destination` support. Forced composite PS `0x5006CD700`; captured `v4` after PCs `0x2C`, `0x2C0`, and `0x4B4`, plus `v10` after PC `0x63C`, into `v248..v251`; exported those values and dumped the post-title swapchain. | Post-title channel analysis: PC `0x2C` `v4` is 255 over all 3,686,400 pixels; PC `0x2C0` `v4` is also 255 everywhere; PC `0x4B4` `v4` is only 0 or 1 (`3,346,788` nonzero after scaling); PC `0x63C` `v10` quantizes to zero in the 2-bit alpha channel. A separate RGB capture at PC `0x63C` retained only 7,170/8,294,400 source pixels and 1,179/3,686,400 presented pixels. | EXEC, sampling, and the first conditional color adjustment are intact. The first large magnitude collapse occurs between PCs `0x2C0` and `0x4B4`; bracket the calculations at PCs `0x40C`, `0x450`, and `0x4B4`. | `/tmp/astro-vcmpx-long-pc063c-rgb-present.log`, `/tmp/astro-vcmpx-long-checkpoints.log`, `/tmp/astro-vcmpx-long-checkpoints-dumps-1/present-0008-2560x1440-B8G8R8A8Unorm.bgra`. |
| E41 | The first magnitude collapse is at or after PC `0x40C`. | Forced composite PS; captured `v4` after PCs `0x40C`, `0x450`, and `0x4B4` into RGB, with the known-good PC `0x2C` sample in alpha. | In the stable post-title dump, PC `0x40C`, `0x450`, and `0x4B4` are all only 0 or 1 in 8-bit presentation (`3,346,788`, `3,344,892`, and `3,346,788` nonzero pixels respectively), while the sample remains 255 over all pixels. No instruction writes `v4` between the known-good PC `0x2C0` checkpoint and PC `0x40C`. | PC `0x40C` is the first collapsing instruction: `VMadF32 v4 <- v9,v4,v11`. Capture `v9` after its PC `0x31C` image load and `v11` after PC `0x3F8` to identify the bad input. | `/tmp/astro-vcmpx-long-math.log`, `/tmp/astro-vcmpx-long-math-dumps-1/present-0008-2560x1440-B8G8R8A8Unorm.bgra`. |
| E42 | PC `0x40C` collapses because either image-load multiplier `v9` or addend `v11` is wrong. | Forced composite PS; captured PC `0x31C` `v9`, PC `0x3F8` `v11`, PC `0x40C` result `v4`, and the PC `0x2C` input into RGBA. The first boot crashed before title and was discarded; the identical retry reached a stable title loop. | Post-title distribution: multiplier `v9` is zero over all 3,686,400 pixels; addend `v11` and result `v4` are identical 0-or-1 values in 8-bit presentation; the sampled input remains 255 everywhere. The PC `0x31C` descriptor resolves to a 1x1 fmt14 image whose traced texel begins `0000803F` (float one), written by compute shader `0x500757800`. | The bad input is conclusively PC `0x31C` `ImageLoad`, not PC `0x40C` FMA or its addend. Fix generic image-load result/component handling or the storage image view/cache binding, then validate original shaders. | `/tmp/astro-vcmpx-pc040c-inputs-retry.log`, `/tmp/astro-vcmpx-pc040c-inputs-dumps-2/present-0008-2560x1440-B8G8R8A8Unorm.bgra`, `/tmp/astro-final-ps-dump.log`. |
| E43 | The PC `0x31C` image load is zero because its type-8 1D descriptor is rejected before host-resource creation. | Classified `ImageLoad*` as storage operations, lowered them with `OpImageRead`, added generic type-8 1D acceptance to `TryCreateVulkanGuestDrawTexture`, and traced address `0x556760000`. | Before 1D acceptance the targeted initialization trace never ran because the descriptor returned a zero fallback at the type check. After acceptance, the first two bindings report `upload_known=False read=True nonzero=True initial_bytes=16`; later bindings correctly reuse the pending image. Two title probes then hit the same guest native exception at `0x80129CDEB` on `RoomLoad_ATQT` immediately after the second title load, before the decisive final-title draw. | The lost guest seed is explained and the generic 1D path preserves it. The two black capture runs are non-evidence because both terminated at the known guest-side crash boundary; retry with minimal tracing and validate PC `0x31C` `v9` on a stable title loop. | `/tmp/astro-storage-init-decision-retry.log`, `/tmp/astro-imageload-1d-probe.log`, `/tmp/astro-imageload-1d-probe-retry.log`. |
| E44 | Repeated `RoomLoad_ATQT` crashes are caused by unresolved `sce::Json` methods returning error code `0x80020002` where the game expects references/pointers. | Used `Firmware/nids.csv` and the public VitaSDK `sce::Json` ABI to implement 32-byte `Value` objects, 8-byte `String` objects, parsing, type/count/index/assignment access, numeric references, string conversion, `c_str`, and constructors/destructors with guest-backed storage. | The old crash at `0x80129CDEB` disappeared. The loader parsed `[{"anim":"play_product",...}]`, followed `link`, `anim`, `delay`, and `duration`, then parsed hundreds of material/marker fields. The log reached `GAME: Level has started: title_controller_ship` without an exception. During the transition the original shaders rendered the first 3D title-scene geometry and then a fully recognizable centered PlayStation Studios logo. Both frames are dark blue with vertical striping instead of their intended color. | JSON was a real independent title-load blocker and is resolved for the exercised ABI. Recognizable boot content proves original geometry, rasterization, depth, and much of the title graph are working. The next controlled experiment is a clean sustained run through this logo; only if it stalls or returns to black should the final PC `0x31C`/`0x40C` capture be repeated. | `/tmp/astro-json-fix-original.log`, `/tmp/astro-json-array-original-retry.log`, `/tmp/astro-json-numeric-original.log`, `artifacts/astro-bot/first-3d-title-geometry.png`, `artifacts/astro-bot/playstation-studios-logo.png`. |
| E45 | A clean original-shader run can carry the boot logo into the live title level and remain stable long enough to determine whether it transitions naturally. | Closed all SharpEmu processes; launched only with `SHARPEMU_IGNORE_STACK_CHK=1 SHARPEMU_IGNORE_INT41=1 SHARPEMU_GPU_WAIT_MODE=force SHARPEMU_OVERLAY=0`; used no shader, depth, color, VGPR, export, or JSON diagnostics. | The process reached `Level has started: ps_logo`, loaded `title_controller_ship` twice, and reached `GAME: Level has started: title_controller_ship`. The PlayStation Studios logo remained recognizable but blue/striped for more than 60 seconds without transitioning. The process later ended with host exit code 1 and no managed fatal marker or macOS DiagnosticReports entry. The attempted Cross press is non-evidence because SharpEmu had already exited before focus could be assigned. | The JSON/title-loader path is reproducible and the logo presentation is stable, but the boot-to-title visual transition does not complete. Repeat the clean run once to classify the unreported host exit; if it survives, capture the fixed PC `0x31C`/`0x40C` values and the final present source during the stalled logo. | `/tmp/astro-post-json-clean.log`, `artifacts/astro-bot/playstation-studios-clean-run.png`. |
| E46 | The merged `codex/astro-bot-menu-progress` tree still boots ASTRO BOT through the title loader. | Built merge commit `b2705da` for `osx-x64`; launched with only `SHARPEMU_IGNORE_STACK_CHK=1 SHARPEMU_IGNORE_INT41=1 SHARPEMU_GPU_WAIT_MODE=force`. The first run used a fresh output directory; the second added the same ignored app-local `libvulkan.1.dylib` and `libMoltenVK.dylib` symlinks used by the experiment checkout. | The first run continued guest execution headlessly but logged `No x86-64 Vulkan loader` and `Vulkan VideoOut presenter failed`, which macOS displayed as an unresponsive application. With the universal MoltenVK links present, the loader was preloaded, GLFW was wired to MoltenVK, the window opened, the PlayStation-symbol intro rendered with the known blue/vertical-stripe defect, and the live process reached `GAME: Level has started: title_controller_ship`. | The source merge preserves the working ASTRO boot/title path. The apparent post-merge hang was a missing ignored runtime artifact, not a renderer or guest regression. Fresh worktrees need a reproducible universal-MoltenVK provisioning step before visual tests. | `/tmp/astro-merged-smoke.log`, `/tmp/astro-merged-smoke-with-vulkan.log`, `artifacts/astro-bot/merged-branch-ps-logo.png`. |
| E47 | The merged branch can reproduce an on-screen window after the user's Dock entry reported `Application Not Responding`. | Verified both app-local Vulkan links, then ran two clean launches. The first used the three E46 compatibility flags; the retry also set `SHARPEMU_OVERLAY=0`. | The first launch initialized Vulkan and presented its splash but exited during guest font setup with `.NET Invalid Program: attempted to call a UnmanagedCallersOnly method from managed code`. The retry stayed alive beyond three minutes, exposed an on-screen 1280x748 window backed by a 2560x1440 presenter, reached `Level has started: ps_logo` and `GAME: Level has started: title_controller_ship`, and presented a black title-level frame with the statistics overlay. It later exited on guest illegal instruction `0xC000001D` at `RIP=0x8000082E6` on the emulation thread. | The user's missing window was an exited/failed launch, not a persistent inability to create a macOS window. Boot-to-title remains reproducible, but sustained stability still fails and the title frame is visually incorrect. Do not attribute the longer retry solely to `SHARPEMU_OVERLAY=0` until it is controlled independently. | `/tmp/astro-merged-relaunch.log`, `/tmp/astro-merged-relaunch-overlay-off.log`, `/tmp/astro-merged-title-live.png`. |

## Controlled matrix

All runs use immediate target readback and close every SharpEmu process first.

| Case | Pixel shader | Depth | Expected control | Result |
| --- | --- | --- | --- | --- |
| M1 | Original | Original | Black baseline | Black: both targets uniform zero |
| M2 | Solid magenta | Original | Black | Black: both targets uniform zero |
| M3 | Solid magenta | Disabled only for title | Magenta MRT0 | MRT0 full magenta; MRT1 zero as expected for the single-output fixed fragment |
| M4 | Original | Disabled only for title | Decides whether the translated PS works | Black: both targets uniform zero; pixel translation is a second blocker |

| E48 | The ASTRO branch can absorb current `par274/main` without losing macOS boot compatibility. | Merged upstream `9d88542` into ASTRO `998b5a2` as a true two-parent merge. Resolved 36 conflicted files; retained the ASTRO native/macOS, AGC, Vulkan/VideoOut, kernel-compatibility, and SELF/TLS loader paths while integrating upstream host-memory interfaces, allocator reuse, JSON value support, and other non-conflicting mainline changes. Added a cross-platform `IHostMemory` adapter, restored the POSIX/Rosetta aligned alternative-allocation path, and made exact-address probing release relocated mappings immediately. | Release `osx-x64` build succeeds with 3 existing `CS0649` warnings and 0 errors. `SharpEmu.Libs.Tests` passes 26/26. The first attempts exposed and then resolved `HostPlatform` Windows-only construction, `sceKernelMapNamedDirectMemory` `NOT_FOUND`, and an upstream SELF/TLS startup regression. | The sync is source- and test-valid, but a logical boot milestone is not proof of visible output. Keep the ASTRO SELF/TLS loader and POSIX allocation compatibility when rebasing future upstream memory/loader work. | `/tmp/astro-main-sync-smoke-2.log`, `/tmp/astro-main-sync-memory-trace.log`, `/tmp/astro-main-sync-smoke-6.log`. |
| E49 | The black window observed after the upstream sync is a new merge regression. | Ran an A/B with identical game and compatibility flags: merged staging tree versus detached pre-sync checkpoint `998b5a2`; both used the same universal MoltenVK runtime links. Captured the pre-sync desktop after its log reached `GAME: Level has started: ps_logo` and loaded `title_controller_ship`, then left it running longer. | The control was black at the first capture, then visibly played the PS logo animation; its window closed only because the test process was deliberately terminated. The initial merged build remained black, never emitted `Level has started: title_controller_ship`, and fell into the audio-assert loop. | The regression was real despite matching early log milestones. Do not classify visual state from `ps_logo` load/start alone; require decoded frames or direct observation. | `/tmp/astro-main-sync-visual-long.log`, `/tmp/astro-before-sync-visual-control.log`, `/tmp/astro-before-sync-visual-control.png`. |
| E50 | Upstream's conflicted AvPlayer implementation is responsible for the black PS intro and stalled title transition. | Compared the merge against `998b5a2`: upstream replaced roughly 1,200 lines of ASTRO's FFmpeg-backed decode, guest-frame allocation, callback, and upload path with a minimal stub. Restored the ASTRO `AvPlayerExports.cs`. Also allowed bundled libc LLE `fputc`/`fwrite` to retain precedence, matching the control's import routing. | The restored merged build opens `data/prein/video/ps_studio_armadillo.mp4`, starts FFmpeg, produces timestamped 3840x2160 video frames, posts the completion event, and reaches `GAME: Level has started: title_controller_ship`. The pre-sync control visibly rendered the same PS logo sequence. | Preserve the full ASTRO AvPlayer implementation when syncing current upstream. This recovers the known visual/title transition while retaining the other upstream host-memory, allocator, GUI, HLE, and test changes. The title level itself can still be black and remains the next rendering target. | `/tmp/astro-main-sync-avplayer.log`, `/tmp/astro-main-sync-final-visual.log`, `/tmp/astro-main-sync-avplayer-60s.png`. |
| E51 | The merged and published build can sustain an original-shader run through the restored PS video and expose the first post-logo presentation failure without diagnostic shader replacement. | Launched merge commit `aaa136b` with only the required compatibility flags plus present-only guest-image tracing and a swapchain dump every 30 presents. No forced color, depth bypass, shader override, or VGPR-export control was enabled. | The full 8.5-second `ps_studio_armadillo.mp4` decoded, `GAME: Level has started: title_controller_ship` appeared, and `worldmap` began loading while SharpEmu remained alive. The last title present changed to a uniform-black 2560x1440 frame (`0/3,686,400` nonblack pixels, hash `0x52B186BB0E2C2325`). Earlier stable presents contained only the known 4,823-pixel two-scanline strip. | The post-logo blocker is the final title presentation path, not AvPlayer, title loading, process lifetime, or window creation. Re-run the final PS `0x500640200` PC `0x31C`/`0x40C` capture against this stable black state and correlate those values with the presented source. | `/tmp/astro-post-logo-baseline.log`, `/tmp/astro-post-logo-baseline-dumps/`, `/tmp/astro-post-logo-baseline-latest.png`. |
| E52 | The post-merge black title still originates at the final PS `0x500640200` PC `0x31C` image load rather than later FMA math. | Forced only composite PS `0x5006CD700` to magenta, captured PC `0x31C` `v9`, PC `0x3F8` `v11`, PC `0x40C` `v4`, and PC `0x2C` `v4` into RGBA, then exported those registers through the final PS and dumped the stable post-title present. | The known-good PC `0x2C` sample was 255 over all 3,686,400 pixels. The PC `0x31C` multiplier was zero over every pixel. The PC `0x3F8` addend and PC `0x40C` result were both the same quantized value 1 over the 3,346,788 active pixels. The process reached `title_controller_ship` and `worldmap` without a fatal marker. | `ImageLoad` at PC `0x31C` remains the first bad value after the upstream merge; the FMA is behaving consistently with its zero multiplier. Read back guest image `0x556760000` at the final-shader boundary to distinguish lost contents from a wrong storage-image view/dimension/binding. | `/tmp/astro-post-logo-pc31c.log`, `/tmp/astro-post-logo-pc31c-dumps/present-0021-2560x1440-B8G8R8A8Unorm.bgra`. |
| E53 | The existing address- and shader-filtered guest-image tracing will read back `0x556760000` immediately after compute shader `0x500757800`. | Launched with `SHARPEMU_TRACE_GUEST_IMAGES=0x556760000`, `SHARPEMU_TRACE_GUEST_IMAGE_SHADER_ADDRS=0x500757800`, compute/storage diagnostics, and Vulkan-resource logging. | The run reached `GAME: Level has started: title_controller_ship`, loaded `worldmap`, and logged 606 references to compute shader `0x500757800`, but emitted zero `vk.guest_image` readbacks. Code inspection showed that the address belongs in `SHARPEMU_TRACE_GUEST_IMAGE_ADDRS`; `SHARPEMU_TRACE_GUEST_IMAGES` only accepts `1`, `present`, or `every:N[@M]`. | This run is configuration evidence, not image-content evidence. The trace plumbing was not exercised because the address filter variable was wrong. Repeat with `SHARPEMU_TRACE_GUEST_IMAGES=1` and `SHARPEMU_TRACE_GUEST_IMAGE_ADDRS=0x556760000`, retaining the compute-shader filter. | `/tmp/astro-post-logo-storage-readback.log`. |
| E54 | Corrected address filtering will show whether compute shader `0x500757800` leaves a valid Vulkan texel in `0x556760000`. | Used `SHARPEMU_TRACE_GUEST_IMAGES=1`, `SHARPEMU_TRACE_GUEST_IMAGE_ADDRS=0x556760000`, and `SHARPEMU_TRACE_GUEST_IMAGE_SHADER_ADDRS=0x500757800`, plus the existing compute/storage diagnostics. | The first resolved bindings correctly found nonzero guest memory and staged all 16 logical bytes (`upload_known=False read=True nonzero=True initial_bytes=16`). The completed-dispatch Vulkan readback was nevertheless all zero: `nonzero_bytes=0/16`, `nonblack_pixels=0/1`, center all zero, hash `0x88201FB960FF6465`. No fatal marker occurred. | The final PS and PC `0x40C` FMA are downstream victims: the real Vulkan storage image is already zero after its compute writer. Distinguish a failed initial upload from a compute `ImageLoad`/`ImageStore` overwrite by running the same resource setup with only the targeted dispatch instruction suppressed. | `/tmp/astro-post-logo-storage-readback-corrected.log`. |
| E55 | Suppressing only the targeted dispatch instruction will preserve the nonzero staged texel if compute execution is what clears it. | Temporarily added `SHARPEMU_SKIP_COMPUTE_EXECUTE_CS` after resource creation/upload recording and before `vkCmdDispatch`; ran it for `0x500757800` with the E54 readback filters. The first logging-heavy attempt exited before the target and was discarded; the minimal retry reached 40 target submissions. The diagnostic control was removed immediately after the run. | Even without executing the compute shader, `0x556760000` read back as the same all-zero texel. Inspection found that `CreateComputeDispatchResources` pre-resolves storage bindings with `ResolveStorageImageResource`, which allocates/stages the nonzero upload, sets `InitialUploadPending`, and then discards that temporary `TextureResource`. The retained second-pass resource sees the pending flag and has `NeedsUpload=false`, so `RecordTextureUploads` never records the copy. | Root cause confirmed: storage image staging is lost during the compute pre-pass. Pre-resolve only the shared `GuestImageResource`, then let the retained second pass create and own the staging resource. This must be fixed before changing 1D SPIR-V or Vulkan dimensions. | `/tmp/astro-storage-upload-no-dispatch-retry.log`; discarded `/tmp/astro-storage-upload-no-dispatch.log`. |
| E56 | Pre-resolving only the shared guest image will preserve the retained texture resource's initial upload through the real compute dispatch. | Changed the storage pre-pass in `CreateComputeDispatchResources` from `ResolveStorageImageResource` to `ResolveStorageGuestImage`; rebuilt Release and repeated E54 with the real compute shader enabled. | The post-dispatch readback changed from all zero to the exact expected 16-byte texel: `0000803F 000080BF 0000003F EC51383E`, `nonzero_bytes=9/16`, `nonblack_pixels=1/1`, hash `0xF7AC1C96716E5A89`. No fatal marker occurred. | The generic storage upload lifecycle bug is fixed. The real compute writer preserves valid data, so the final PS PC `0x31C` now has a valid Vulkan source without requiring a true 1D rewrite. Validate the original post-logo presentation with only normal compatibility flags and present dumps. | `/tmp/astro-storage-upload-fix-readback.log`. |
| E57 | Restoring the 1x1 storage texel will make the original post-logo title/menu frame visible. | Ran original shaders with only the required compatibility flags, present tracing, and a swapchain dump every 30 presents; no shader, color, depth, VGPR, export, or compute override was enabled. | The run decoded the PS video, reached `GAME: Level has started: title_controller_ship`, loaded `worldmap`, and stayed alive. Before the fix-equivalent title boundary the swapchain was uniformly black (`0x52B186BB0E2C2325`); afterward it became fully nonblack with changing hashes including `0x2E6AAA4F6F886D25`, `0x3CE168D95EB747A7`, and `0x94078CE07FB79F0F`. Visual inspection shows an animated but badly blue/outlined full-screen image with diagonal and horizontal bands plus a ghosted PlayStation Studios logo, not the real menu. | The storage lifecycle fix materially advances the original rendering graph and removes uniform black, but the frame is still composition/format-corrupted. Trace the two alternating 3840x2160 A2R10 present images (`0x507410000`, `0x5093F0000`) before swapchain conversion to decide whether corruption is already in guest output or introduced by presentation. | `/tmp/astro-storage-fix-original.log`, `/tmp/astro-storage-fix-original-dumps/`, `/tmp/astro-storage-fix-original-latest2.png`. |
| E58 | Generic draw-target occurrence tracing can capture the alternating present images late in the title loop without present-mode tracing. | Used `SHARPEMU_TRACE_GUEST_IMAGE_ADDRS=0x507410000,0x5093F0000`, occurrence 225, and a dump directory; no rendering overrides. | The run reached `title_controller_ship`, loaded `worldmap`, and stayed alive, but emitted no target readbacks or dumps. Code inspection confirmed that these flip images are consumed through the direct-presentation path; its readback checkpoints are controlled separately by present mode at direct presentation counts 1, 30, 120, and 600. | This configuration cannot distinguish guest-output corruption from presentation corruption. Add a generic address/occurrence filter to direct-presentation readback rather than waiting to frame 600 or broadening draw-target diagnostics. | `/tmp/astro-present-target-occurrence.log`; empty `/tmp/astro-present-target-occurrence-dumps/`. |
| E59 | An address/occurrence filter at direct presentation will reveal whether the corruption exists before the swapchain conversion. | Added generic `SHARPEMU_TRACE_PRESENTED_GUEST_IMAGE_ADDRS` and `SHARPEMU_TRACE_PRESENTED_GUEST_IMAGE_OCCURRENCE` controls; captured occurrence 220 for both alternating buffers after `worldmap` loaded. | Both 3840x2160 A2R10 images were fully nonblack and varied (`0x507410000` hash `0xE374308D0966354D`, center `47E261C9`; `0x5093F0000` hash `0xBEE6A6EBC9393C14`, center `47F2A1C9`). Decoding the raw A2R10 data shows the same blue/outlined diagonal corruption and ghosted PlayStation Studios logo seen in the swapchain. | Swapchain blit/presentation is exonerated. The corruption is already in the final guest output written by PS `0x500640200`. Capture its main 2432x1368 R16G16B16A16 input `0x53B9F0000` after producer PS `0x5003DD500`; compare that source with the final export before changing presentation formats. | `/tmp/astro-direct-present-readback.log`, `/tmp/astro-direct-present-readback-dumps/`, `/tmp/astro-present-507410000.png`, `/tmp/astro-present-5093f0000.png`. |
| E60 | Shader/address/occurrence filters can capture `0x53B9F0000` specifically after producer PS `0x5003DD500`. | Used target address `0x53B9F0000`, shader address `0x5003DD500`, occurrence 220, and a dump directory; no rendering overrides. | The run reached `title_controller_ship`, loaded `worldmap`, and stayed alive, but emitted no readback. The offscreen-graphics path calls `GetTraceImages(resources, targets)` without a shader address, so enabling `SHARPEMU_TRACE_GUEST_IMAGE_SHADER_ADDRS` rejects every candidate as shader address zero. | This is configuration/tooling evidence, not source-image evidence. Repeat without the shader filter; the target receives two alternating writes per frame, so use total address occurrence 440 to land on the 2432x1368 fmt12 write near the desired title frame. | `/tmp/astro-final-input-readback.log`; empty `/tmp/astro-final-input-readback-dumps/`. |
| E61 | Address occurrence 440 without a shader filter will capture the final PS's 2432x1368 fmt12 input after `worldmap` loads. | First tried occurrence 880, which did not fire during the observed title window and was discarded; then used occurrence 440 with only the target address filter. | Occurrence 440 fired immediately after `worldmap`, but it selected the alternating 1920x1080 fmt3/R8G8 write, not the desired fmt12 variant: `nonzero_bytes=1,306,197/4,147,200`, `nonblack_pixels=1,098,737/2,073,600`, center `00FF`, hash `0xDED68A7D075EBC20`. | The timing is correct and the variants alternate on adjacent target occurrences. Repeat at occurrence 441 to capture the following 2432x1368 R16G16B16A16 write consumed by final PS `0x500640200`. | `/tmp/astro-final-input-readback-440.log`, `/tmp/astro-final-input-readback-440-dumps/`; discarded `/tmp/astro-final-input-readback-retry.log`. |
| E62 | Adjacent occurrence 441 will select the 2432x1368 fmt12 variant at the same post-worldmap point. | Repeated the address-only capture at occurrence 441. | It again selected the 1920x1080 R8G8 variant, now with `nonzero_bytes=1,186,174/4,147,200`, `nonblack_pixels=974,025/2,073,600`, center zero, and hash `0xDDA80F0FAF388079`. The same guest address has multiple consecutive writes and cannot be selected reliably by parity. | Add optional width/height filters to generic guest-image tracing so occurrence counts only the exact 2432x1368 resource variant. This is more deterministic than guessing neighboring occurrences or propagating shader addresses through the draw record. | `/tmp/astro-final-input-readback-441.log`, `/tmp/astro-final-input-readback-441-dumps/`. |
| E63 | Exact 2432x1368 width/height filtering plus occurrence 220 will capture the desired fmt12 variant late in the title loop. | Added generic `SHARPEMU_TRACE_GUEST_IMAGE_WIDTH`/`HEIGHT` filters and ran address `0x53B9F0000`, size 2432x1368, occurrence 220. The first launch hit an early guest access violation and was discarded; the identical retry reached and loaded `worldmap`. | The exact counter did not reach 220 before the retry later ended on the known guest illegal-instruction boundary. No readback fired, so the run does not classify source-image contents. | Occurrence 220 is unnecessarily late once nonmatching variants are excluded. Use existing interval mode with the exact address/size filters to capture every 30th matching fmt12 write, yielding several checkpoints without guessing the total count. | `/tmp/astro-final-input-exact-retry.log`; discarded `/tmp/astro-final-input-exact.log`; empty dump directories. |
| E64 | Exact address/extent interval tracing will show how the final PS's half-float source changes across the title transition. | Used `SHARPEMU_TRACE_GUEST_IMAGES=every:30` with address `0x53B9F0000`, exact 2432x1368 filters, and raw dumps. | Three checkpoints fired: two before `ps_logo` and one immediately after `GAME: Level has started: title_controller_ship`. All were identical RGB-zero/alpha-one images (`nonzero_bytes=3,326,976/26,615,808`, `nonblack_pixels=0/3,326,976`, center `000000000000003C`, hash `0xF0AF3F03C2E22325`). No fourth checkpoint appeared during more than 120 seconds after `worldmap` loaded, even though the A2R10 output became animated and corrupted. | The previously identified source remains black through the handoff and then stops receiving enough matching writes. Before blaming tone-map math, trace live bindings for final PS `0x500640200` to determine whether its PC `0x2C` descriptor switches to a different address/extent in the worldmap loop. | `/tmp/astro-final-input-interval.log`, `/tmp/astro-final-input-interval-dumps/`. |
| E65 | A targeted final-pixel-shader trace will identify the live PC `0x2C` source after `worldmap`. | Added generic `SHARPEMU_TRACE_PIXEL_SHADER_ADDRESS`, emitting render-target and texture-binding records only for the selected PS; ran it for `0x500640200` through `worldmap`. | PC `0x2C` used several rotating 2432x1368 fmt12 buffers: 38 observed bindings to `0x53B9F0000`, 6 to `0x53D4A0000`, and 257 to `0x53AA00000` before the final transition. After `GAME: Level has started: title_controller_ship` and `worldmap` load completion, every latest binding switched to `0x53C420000`. Final targets still alternated `0x507410000`/`0x5093F0000`. | The old half-float source trace was stale after the worldmap switch. Read back `0x53C420000` at 2432x1368; do not attribute the corrupt A2R10 output to tone-map math until this live source is classified. | `/tmp/astro-final-ps-live-bindings.log`. |
| E66 | The first 2432x1368 write to live source `0x53C420000` will show whether the final tone mapper receives a real title image. | Used exact address/extent filters and occurrence one for `0x53C420000`; no rendering overrides. | The capture fired immediately before `worldmap` document completion. The source is not a real scene: 3,320,741 of 3,326,976 pixels are uniform half-float `(9992,9992,9992,1)`; 6,015 pixels are `(0,0,0,1)` and 220 are `(9968,9968,9968,1)`. All exceptional pixels lie in a 61x115 region at the far right (`x=2371..2431`, `y=575..689`). GPU summary: `nonblack_pixels=3,320,961/3,326,976`, center `E170E170E170003C`, hash `0xBA45F0CC0C0E05EC`. | The final PS is consuming an already-invalid near-uniform HDR source. Continue backward through its fullscreen producer, historically ES `0x5003DCB00` / PS `0x5003DD500`, and identify that producer's live 1920x1080 input after worldmap. | `/tmp/astro-live-final-input.log`, `/tmp/astro-live-final-input-dumps/0001-0x000000053C420000-2432x1368-R16G16B16A16Sfloat.rgba`. |
| E67 | Historical producer PS `0x5003DD500` writes the live post-worldmap source `0x53C420000`. | Ran targeted pixel-shader binding/target tracing for `0x5003DD500` through worldmap. | The pass remained active but every latest target was `0x53B9F0000`, sourced from 1920x1080 `0x53D410000`; it never wrote `0x53C420000` in this run. | Corrected conclusion: buffer rotation changes the producer shader as well as addresses, so the historical writer cannot be assumed. Add address-targeted render-target tracing and identify the actual PS that writes `0x53C420000`. | `/tmp/astro-final-input-producer-bindings.log`. |
| E68 | Exact render-target-address tracing will identify the actual writer of live source `0x53C420000`. | Added generic `SHARPEMU_TRACE_RENDER_TARGET_ADDRESS` and ran it for `0x53C420000` through worldmap. | Every observed 2432x1368 fmt12 write used ES `0x50065C000` and PS `0x50065CA00`, a three-vertex rectangle draw. The pass repeated stably after worldmap. | Actual producer identified. Trace PS `0x50065CA00` directly to enumerate its sampled source(s) and determine whether the `9992` field is sampled, computed, or preserved clear data. | `/tmp/astro-live-final-source-writer.log`. |
| E69 | Targeted tracing of producer PS `0x50065CA00` will enumerate every live input used to write invalid HDR source `0x53C420000`. | Ran with only the required compatibility flags plus `SHARPEMU_TRACE_PIXEL_SHADER_ADDRESS=0x50065CA00`. The first attempt terminated before the target pass with a separate .NET `Invalid Program: attempted to call a UnmanagedCallersOnly method from managed code` fatal inside `TryEmitScalarMemory`; an identical clean-process retry continued through `ps_logo`, `title_controller_ship`, and `worldmap`. | The retry observed 28 target writes and 56 bindings by the first post-worldmap checkpoint. Every target write was 2432x1368 fmt12 at `0x53C420000`. Each draw sampled exactly two images: PC `0x44` read `0x5367F0000`, 1920x1080 fmt5; PC `0x60` read `0x53AA00000`, 2432x1368 fmt12. Both bindings remained stable after worldmap. | The producer is a two-input fullscreen composition pass. The aborted first attempt is not shader evidence. Read back both live inputs after worldmap; whichever first contains the near-uniform `9992` field identifies the next upstream boundary, while plausible inputs would instead localize the bug to PS `0x50065CA00` translation or draw state. | `/tmp/astro-live-hdr-producer-bindings.log`, `/tmp/astro-live-hdr-producer-bindings-retry.log`. |
| E70 | Existing address-filtered guest-image tracing can capture the two sampled inputs through their upstream target writes. | Ran `SHARPEMU_TRACE_GUEST_IMAGES=every:30` with only input addresses `0x5367F0000,0x53AA00000` through `title_controller_ship` and completed `worldmap` loading. | The emulator remained alive with no fatal marker, but produced zero readback records and zero dumps, including a full post-worldmap interval. Code inspection showed `GetTraceImages` includes render targets and storage textures only; these ordinary sampled images never entered the candidate set in this graph. | This is a diagnostic coverage failure, not content evidence. Extend generic tracing so explicit address filters may select sampled guest images, while leaving broad tracing limited to targets/storage resources. | `/tmp/astro-hdr-producer-input-readback.log`; empty `/tmp/astro-hdr-producer-input-dumps/`. |
| E71 | Explicit address filtering of sampled `GuestImageResource` aliases will capture producer inputs at occurrence one. | Extended `GetTraceImages` to admit sampled guest images only when `SHARPEMU_TRACE_GUEST_IMAGE_ADDRS` is explicitly set; rebuilt Release and ran both live addresses with occurrence one through completed `worldmap` loading. | The first interpretation incorrectly reported zero readbacks because it searched for `guest_image_contents` rather than the actual `vk.guest_image` marker, and it set `SHARPEMU_TRACE_GUEST_IMAGE_DUMP_DIR` instead of the implemented `SHARPEMU_GUEST_IMAGE_DUMP_DIR`. | Corrected conclusion: this run was misconfigured for artifacts and cannot classify alias contents. The sampled-alias extension is functional; repeat with the correct dump directory and inspect `vk.guest_image` records. | `/tmp/astro-hdr-sampled-input-readback.log`; empty `/tmp/astro-hdr-sampled-input-dumps/`. |
| E72 | Correctly configured sampled-image tracing will classify live producer input `0x53AA00000` before PS `0x50065CA00` consumes it. | Ran both input addresses with the correct `SHARPEMU_GUEST_IMAGE_DUMP_DIR`; added address-filtered standalone upload summaries as fallback, though this source resolved through the guest-image alias path. | The run captured multiple 2432x1368 R16G16B16A16 source frames at `0x53AA00000`. A latest frame had only three unique pixels: 1,663,047 `(9992,9992,9992,1)` (`70e1,70e1,70e1,3c00`), 1,661,211 `(0,0,0,1)`, and 2,718 `(9968,9968,9968,1)` (`70de,...`). GPU summary: `nonblack_pixels=1,665,765/3,326,976`, center `E170E170E170003C`, hash `0x61D9CE024A3B789C`. | The invalid field exists one full pass upstream of `0x53C420000`; PS `0x50065CA00` is a downstream compositor, not the first corruptor. Identify the exact 2432x1368 writer of `0x53AA00000` and trace its inputs. | `/tmp/astro-hdr-texture-upload-readback.log`, `/tmp/astro-hdr-texture-upload-dumps/0186-0x000000053AA00000-2432x1368-R16G16B16A16Sfloat.rgba`. |
| E73 | Exact render-target tracing will identify the 2432x1368 producer of `0x53AA00000`. | Two identical launches terminated before GPU work on the known guest TBB access violation at `0x800A4D571`; the third reached and completed worldmap while tracing the address. Cross-checked existing address-filtered AGC compute logs. | The successful run recorded thousands of graphics writers at reused 1920x1080 variants but zero 2432x1368 graphics writes. Existing compute logs show two nominal storage bindings: CS `0x50068EF00` and `0x500657200`. The latter is a diagnostic false writer because its binding is `ImageLoad`; its actual `ImageStore` is a 60x33 image. CS `0x50068EF00` is the real 152x86-group upscaler, repeatedly loading live source `0x537060000` and storing `0x53AA00000` at 2432x1368. | The corrupt buffer is a compute output, not a graphics target. Capture `0x537060000` immediately before CS `0x50068EF00`; also fix writer diagnostics later so read-only storage bindings are not labeled writers. | `/tmp/astro-upstream-hdr-source-writer-retry2.log`; discarded `/tmp/astro-upstream-hdr-source-writer.log` and `-retry.log`; `/tmp/astro-compute-68ef-title-long.log`. |
| E74 | The first live 2432x1368 readback of upscaler input `0x537060000` will distinguish an upscaler bug from upstream corruption. | Used exact address/extent filters and occurrence one with the correct dump directory; no rendering overrides. Cross-checked existing graphics-writer and targeted draw logs for the captured address. | The source is already invalid: exactly 1,665,765 pixels are half-float `(10000,10000,10000,1)` (`70e2,70e2,70e2,3c00`) and 1,661,211 are `(0,0,0,1)`, with no other values. GPU summary: center `E270E270E270003C`, hash `0x24AD51351F8F6F67`. Its actual writer is MRT PS `0x5006CD700` / ES `0x5006CCF00`, a 3-vertex primitive `0x4` draw; CS `0x50068EF00` only reads it. Historical targeted draws show PS `0x5006CD700` sampling 1920x1080 sources and another 2432x1368 source while writing this target. | The upscaler preserves an already-corrupt split field. Trace PS `0x5006CD700` live bindings and classify its primary sources; the first plausible-input/invalid-output boundary is now at or upstream of this MRT pass. | `/tmp/astro-upscaler-input-readback.log`, `/tmp/astro-upscaler-input-dumps/0001-0x0000000537060000-2432x1368-R16G16B16A16Sfloat.rgba`, `/tmp/astro-pr149-vertex-raw-force.log`. |
| E75 | Targeted live tracing of PS `0x5006CD700` will identify the inputs that produce the split 10000/black field. | Ran `SHARPEMU_TRACE_PIXEL_SHADER_ADDRESS=0x5006CD700` through sustained title initialization with original shaders. | The pass repeatedly writes MRT0 `0x537060000` and alternating MRT1 `0x564000000`/`0x567FC0000`, all 2432x1368 fmt12. It samples `0x512570000` 1920x1080 at PC `0x60`, `0x53AA00000` 1920x1080 across PCs `0xF8..0x150`, the alternating 2432x1368 history target at PC `0x160`, and a 1x1 fallback at PC `0x950`. Existing current-run capture `0x53AA00000` has a two-value split: 1,036,800 pixels are half-float quiet NaN in every channel (`7e00` x4) and 1,036,800 are all zero. Historical `0x512570000` captures are uniformly zero. Draw ordering shows PS `0x5006CAD00` writes the 1920x1080 `0x53AA00000` source immediately before this pass. | The 10000 clamp is explained by an upstream half-frame NaN source. Capture live inputs to PS `0x5006CAD00` (`0x53BE70000` and `0x514080000`) and determine whether that shader creates the NaNs from valid/black data. | `/tmp/astro-mrt-producer-live-bindings.log`, `/tmp/astro-hdr-texture-upload-dumps/0215-0x000000053AA00000-1920x1080-R16G16B16A16Sfloat.rgba`, `/tmp/astro-pr149-vertex-raw-force.log`. |
| E76 | Exact address/extent occurrence-one captures will classify both inputs to NaN writer PS `0x5006CAD00`. | Traced `0x53BE70000` and `0x514080000` at 1920x1080 with original shaders. | `0x514080000` was captured as R16G16B16A16Sfloat and is uniformly zero (`nonzero_bytes=0`, hash `0xF4C173429798A325`). `0x53BE70000` was captured as R8G8B8A8Unorm and is also uniformly zero (`nonzero_bytes=0`, hash `0x97D30483E5DD6325`). A later mapping check confirmed this is the correct host format for guest format 10/number type 0. | Both live inputs are finite black. PS `0x5006CAD00` is the first confirmed finite-input/NaN-output boundary. | `/tmp/astro-nan-writer-input-readback.log`, `/tmp/astro-nan-writer-input-dumps/`. |
| E77 | Exact format filtering will confirm whether `0x53BE70000` has a hidden B10G11R11 variant. | Added generic `SHARPEMU_TRACE_GUEST_IMAGE_FORMAT`, rebuilt Release, and ran the address/extent with `B10G11R11UfloatPack32` through worldmap. | No resource matched. Code inspection corrected the premise: `GetTextureFormat(10, 0)` maps to `R8G8B8A8Unorm`; B10G11R11 is guest format 6 or 7 with number type 7. | The E76 R8 capture was the exact sampled view, so both inputs are conclusively finite black. Dump PS `0x5006CAD00` guest IR and SPIR-V, then locate the instruction/translation that creates NaNs. | `/tmp/astro-b10-source-readback.log`; empty `/tmp/astro-b10-source-dumps/`. |

| E78 | The E76 allocation-time captures may not represent the images sampled by PS `0x5006CAD00` at its live draw boundary. | Added generic graphics-shader addresses to queued draw trace metadata and passed the active pixel-shader address into `GetTraceImages`; rebuilt self-contained Release and traced `0x53BE70000` plus `0x514080000` with shader filter `0x5006CAD00`. | The prior E76 readbacks occurred around the first presented frame, before the relevant live postprocess. The corrected exact-shader capture still reads both inputs as uniform zero, while `0x53AA00000` immediately contains qNaN output. The dumped IR/SPIR-V lowers `VRcpF32` to IEEE `1.0 / (v8 + v10)`; with both weights zero, the shader itself produces infinity and then `0 * infinity = NaN`. | Corrected conclusion: E76's timing claim was invalid, but the new shader-filtered evidence independently confirms the same live values. Do not add a reciprocal clamp. Trace the upstream 24,192-index writer PS `0x500781200` that should populate main HDR scene target `0x514080000`. | `/tmp/astro-nan-writer-live-input.log`, `/tmp/astro-nan-writer-live-input-dumps/`, `/tmp/astro-5006cad00.spvasm`. |

| E79 | Shader-filtered graphics readbacks must flush at the matched draw to distinguish the individual `0x514080000` passes. | Changed only the explicit shader-filter diagnostic path to flush a nonempty matched readback immediately. Ran the seven-pass HDR history with original shaders, then forced only the fragment shader for target `0x514080000` to solid magenta while retaining the original indexed geometry, depth, viewport, and raster state. Dumped PS `0x500781200` and captured its sampled alpha plus late RGB VGPRs. | With true per-draw readbacks, every original pass leaves `0x514080000` uniformly RGBA zero. The solid-fragment control fills all 2,073,600 pixels with half-float `(1,0,1,1)`, proving geometry coverage, neutral depth, rasterization, MRT routing, and attachment writes. The 1x1 sRGB input is exactly `00 00 00 FF`; PS capture returns sampled alpha `1` but late `v0/v1/v2` RGB all zero. Earlier delayed alpha-9992 captures were post-batch state and are superseded. | The first active rendering failure is inside original PS `0x500781200` or its scalar/interpolated inputs, not depth or geometry. Compare the confirmed `superliminal-launch` implementation, then capture this shader's interpolants and scalar-buffer-derived operands before its first magnitude collapse. | `/tmp/astro-hdr-pass-history.log`, `/tmp/astro-hdr-pass-history-dumps/`, `/tmp/astro-main-writer-solid.log`, `/tmp/astro-main-writer-solid-dumps/`, `/tmp/astro-main-writer-vgpr.log`, `/tmp/astro-main-writer-vgpr-dumps/`, `/tmp/astro-500781200.spvasm`. |

| E80 | The confirmed `xnetcat/superliminal-launch` 3D-menu branch may contain missing rendering changes. | Fetched `xnetcat/sharpemu:superliminal-launch` at `f6e9c6592038` and compared its ancestry and commits with the ASTRO staging branch. | The branch tip is already a full ancestor of the current work; no unique Superliminal commit is missing, and the current branch is 226 commits ahead of it. | Its known 3D support is already integrated. Do not cherry-pick or duplicate that branch; continue with ASTRO's shader-specific evidence. | `refs/remotes/xnetcat/superliminal-launch`, merge-base `f6e9c6592038`. |
| E81 | PS `0x500781200` may receive zero interpolated vertex attributes. | Captured attribute 2 channels after interpolation with `SHARPEMU_CAPTURE_PIXEL_VGPR_POINTS=112:14:248,112:8:249,112:15:250,228:1:251`, forced only the captured registers to MRT0, and used immediate shader-filtered readback. | Every captured pixel is half-float `(1,1,1,1)` across the full 1920x1080 draw. | Basic vertex-to-pixel linkage is not a blanket-zero failure. Resume at the other interpolants and scalar/global-buffer-derived operands used by this shader. | `/tmp/astro-main-writer-attr2.log`, `/tmp/astro-main-writer-attr2-dumps/`. |
| E82 | Updating to the latest `par274/main` may regress the ASTRO boot/logo milestone or the existing test/CI surface. | Merged upstream through `c69ac6d` (PRs #190, #192, #196, #197, #202, #47, and #209), preserved newer ASTRO/POSIX implementations for duplicate squashed-PR conflicts, added a waiter compatibility bridge, normalized lockfiles, and restored missing synthetic shader decoder/harness compatibility. Ran locked restore, Release build, all tests, the shader validator, self-contained `osx-x64` publish, and an original-shader ASTRO smoke run. | Locked restore, build, 26/26 tests, every synthetic shader, and self-contained publish pass. The published binary presents the PS splash, starts `ps_logo`, and loads `title_controller_ship` while remaining alive. | The maintenance merge preserves the achieved boot/logo/title-level milestone and is safe to checkpoint. Correct menu rendering remains the next emulator goal. | `/tmp/astro-upstream-smoke.log`, `/tmp/sharpemu-ci-publish-osx-x64/`. |
| E83 | A clean upstream-quality replay can preserve current `par274/main` behavior without losing the ASTRO milestones. | Replayed the ASTRO changes directly onto `c69ac6d`, restored every upstream SysAbi export, disabled Server GC because its 16 heap reservations overlapped ASTRO's required `0x800000000` image base, removed experiment-only artifacts, and validated from a fresh checkout with original shaders. | The clean checkout restores and builds; 26/26 tests and every synthetic shader pass; all 715 upstream exports remain, plus 152 compatibility exports. ASTRO presents the splash, starts `ps_logo` and `title_controller_ship`, and loads `worldmap`. The final sustained run later ended at the separately tracked guest illegal instruction `0x8000082E6`. | The maintenance integration is validated and ready for a draft upstream review. Keep the intermittent illegal-instruction boundary separate from the rendering investigation. | `/tmp/sharpemu-clean-check-build.log`, `/tmp/sharpemu-clean-check-tests.log`, `/tmp/sharpemu-clean-check-shader.log`, `/tmp/astro-final-pr-smoke.log`. |

## 2026-07-16 producer-chain update

| ID | Hypothesis | Exact change or flags | Observed result | Conclusion | Evidence |
| --- | --- | --- | --- | --- | --- |
| E186 | The strengthened screenshot harness had lost the PS Studios intro. | Stopped at the loose `title_controller_ship` load substring and generated a contact sheet. | The run ended before the level-start milestone and before the normal intro window. | The missing intro conclusion was a harness-timing error. Use the exact `GAME: Level has started: title_controller_ship` milestone. | `artifacts/astro-bot/runs/20260716-*/` E186 run. |
| E187 | A full exact-milestone run will distinguish an intro regression from an early stop. | Used the default exact title milestone, timestamped captures, and no rendering overrides. | PS Studios wordmark matched at about 94.2 seconds; the title level started at about 101.6 seconds. The F1 overlay was visible. | Intro presentation remains present. The visual blocker is after the logo, not presentation starvation. | `artifacts/astro-bot/runs/20260716-154541-e187-title-started-buffer-identity/`. |
| E188 | The final 16 MiB particle/vertex producer may never reach `sceAgcCbDispatch`. | Added an exact leaf-import return-address probe and traced the Emitter helper return `0x806F46D13`. | Two expected `192x1x1` calls occurred, but compute program `0x555F4F500` failed with `error=unterminated`. | Dispatch plumbing is correct. The first concrete producer blocker was shader decoding. | `artifacts/astro-bot/runs/20260716-155057-e188-emitter-dispatch-leaf-probe/attempt-01.log`. |
| E189 | `0x555F4F500` is either a bad data pointer or a valid shader beyond the decoder limit. | Added an exact-address raw shader dump and captured 64 KiB plus the AGC header. Cross-checked the embedded ELF with KawaiiDra and Kyty's public AGC implementation. | Header `0x555F556F0` declares `shader_size=0x61F0`; code begins at `0x555F4F500`; a real `S_ENDPGM` is at offset `0x5A94`. Code plus size lands exactly on the header. | The pointer and upload are correct. SharpEmu's arbitrary 4,096-instruction cap truncated a valid large compute shader. | `artifacts/astro-bot/shader-programs/e189/`, KytyPS5 commit `f743a48c`. |
| E190 | Decoding by the AGC header's exact shader size will restore GpuEmitterSpawn. | Read size at header `+0x44`, keyed the decode cache by `(address,size)`, byte-bounded instruction reads, retained a 64 KiB headerless fallback and 1 MiB safety ceiling, and added long-program/cache-bound tests. | Shader tests pass 13/13. Emitter produces 2,464,700-byte SPIR-V, binds the correct alternating 16 MiB target, and dispatches `192x1x1` with `gpu=True` and `global_writes=True`. Early-boot writeback remained all zero. | The decoder blocker is fixed. The next boundary is producer input/state population or translated Emitter semantics at the title frame. | `artifacts/astro-bot/runs/20260716-160643-e190-emitter-after-size-bound/attempt-01.log`, build `artifacts/astro-bot/build/20260716-160608-osx-x64.log`. |
| E191 | A full contact-sheet run after the Emitter fix will show whether title geometry returns. | Started the normal exact-title harness with F1 overlay and dense timestamped captures. | The run was intentionally stopped when repository synchronization became the priority. | No visual conclusion; do not count E191 as a failed rendering experiment. | Aborted run tag `e191-full-title-after-emitter-size-fix`. |
| E192 | Current `par274/main` can be merged without regressing the validated ASTRO and shader work. | Fetched `origin/main` at `2db1fae` (18 commits beyond the prior merge base); merged it into `codex/astro-playable-next`. Reconciled upstream's bounded Vulkan host-buffer pool and RDNA2 atomics with the ASTRO queue/writeback diagnostics, `DsPermuteB32`, and exact AGC shader-size decoder. | Release `osx-x64` publish succeeds. `SharpEmu.Libs.Tests` passes 191/191, `SharpEmu.ShaderCompiler.Tests` passes 13/13, and the SourceGenerators test project exits successfully. `git diff --check` is clean. | The fork is source- and test-current with `par274/main`; a runtime ASTRO contact-sheet test remains intentionally deferred because E191 was stopped for synchronization. | Merge `7bcbd4e`; `artifacts/astro-bot/build/20260716-162125-osx-x64.log`. |

## Current blocker and next action

The intro is still present, and the GpuEmitterSpawn dispatch now translates and executes against the correct 16 MiB buffer. Its early-boot output is zero. After synchronizing Acelogic `main` with current `par274/main` and rerunning regression tests, repeat the full exact-title contact-sheet run. At the live title frame, trace the two 1.5 MiB ObjectUpdate state buffers, the 96 KiB CPU table, and the alternating 16 MiB Emitter target. If the state inputs are populated but the target remains zero, compare only the address-filtered Emitter branch/store semantics. Do not reopen depth, final pixel, swapchain, or intro-queue theories until this producer chain is resolved.

Keep the intermittent illegal-instruction, TBB access-violation, and `UnmanagedCallersOnly` boundaries separate from the rendering blocker. Do not re-import `superliminal-launch`, and do not add title-specific shader replacements.
